{%- assign headerSnippetPath = "header-password" -%}{%- assign wfPage = "654d36a6d6abfe37729082dc" -%}{%- assign bodyClasses = "" -%}
  <div class="page-wrapper">
    {%- render 'google-icons' -%}
    <header class="header">
      {%- section 'navbar' -%}
    </header>
    <main class="main-wrapper">
      <section class="section container">
        <div class="flex align-center space-between">
          <h4>{{ 't1d0c220d' | t }}</h4>
          <a href="/account/logout" class="btn secondary w-button">{{ 'tn7802fe56' | t }}</a>
        </div>
        <div class="spacer-3"></div>
        <div class="account-container">
          <h5>{{ 'tn72bbb0fb' | t }}</h5>
          <div class="spacer-2"></div>
          <div class="account-labels">
            <div order="fulfillment-status">{{ 't4d6b73d2' | t }}</div>
            <div>{{ 'tn6b8a7b56' | t }}</div>
            <div>{{ 't4d3eb24' | t }}</div>
            <div>{{ 'tn6bcd43ee' | t }}</div>
          </div>
          {%- for order in customer.orders -%}<div class="account-item">
            <a href="{{ order.customer_url }}">{{ order.name }}</a>
            <div>{{ order.created_at | date: "%a, %b %d, %y" }}</div>
            <div>{{ order.total_price | money }}</div>
            <div>{{ order.fulfillment_status_label }}</div>
          </div>{%- endfor -%}
          {%- for order in customer.orders -%}<div class="account-item">
            <a href="{{ order.customer_url }}">{{ order.name }}</a>
            <div>{{ order.created_at | date: "%a, %b %d, %y" }}</div>
            <div>{{ order.total_price | money }}</div>
            <div>{{ order.fulfillment_status_label }}</div>
          </div>{%- endfor -%}
          {%- for order in customer.orders -%}<div class="account-item">
            <a href="{{ order.customer_url }}">{{ order.name }}</a>
            <div>{{ order.created_at | date: "%a, %b %d, %y" }}</div>
            <div>{{ order.total_price | money }}</div>
            <div>{{ order.fulfillment_status_label }}</div>
          </div>{%- endfor -%}
        </div>
        <div class="account-container">
          <div class="flex align-center space-between">
            <h5>{{ 'tn546d647e' | t }}</h5>
            <a href="{{ routes.account_addresses_url }}" class="btn secondary w-button">{{ 't20e22a' | t }}</a>
          </div>
          <div class="spacer-2"></div>
          <div>{{ customer.default_address | format_address }}</div>
        </div>
      </section>
      {%- section 'footer' -%}
    </main>
  </div>
  <script src="https://d3e54v103j8qbb.cloudfront.net/js/jquery-3.5.1.min.dc5e7f18c8.js?site=654ae336a6e86029761320fd" type="text/javascript" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
  <script src="{{ 'first-cup-coffee.js' | asset_url }}" type="text/javascript"></script>
  <script>
  function megaBack() {
  var x = document.getElementsByClassName("mega-menu");
    $('.menu-button').on('click', function(evt) {
  if (window.getComputedStyle(x).display === "block") {
    setTimeout(function() {
        $('.mega-menu_mobile-close').triggerHandler('click');
    }, 005);
});
  }
}
</script>
  <script>
$('.input-number-increment').click(function() {
  var $input = $(this).parents('.input-number-group').find('.input-number');
  var val = parseInt($input.val(), 10);
  $input.val(val + 1);
});
$('.input-number-decrement').click(function() {
  var $input = $(this).parents('.input-number-group').find('.input-number');
  var val = parseInt($input.val(), 10);
  $input.val(Math.max(val - 1, 1));
})
</script>

{{ 'udesly-shopify.min.js' | asset_url | script_tag }}